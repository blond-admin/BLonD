language: python
python:
  - "2.7"
  - "3.4"
install:
  - pip install coveralls,numpy,scipy,matplotlib,h5py,future
before_script:
  - export PYTHONPATH='./'
script:
  - coverage run -m pytest -k 'Test and py' unittests/*/*