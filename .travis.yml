sudo: false
language: python
python:
  - "2.7"
  - "3.4"
install:
  - pip install coveralls numpy scipy matplotlib future
cache:
  pip: true
before_script:
  - export PYTHONPATH='./'
script:
  - coverage run -m pytest -k 'Test and py' unittests/*/*